{
  "name": "whispr",
  "private": true,
  "description": "Repo-root tooling for Whispr. The app itself lives in frontend/ and backend/ — this file only carries the git-hook toolchain (husky + lint-staged) so pre-commit works across the subdir layout.",
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.5",
    "lint-staged": "^15.2.10",
    "prettier": "^3.3.3"
  }
}
